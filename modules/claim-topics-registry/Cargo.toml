[package]
name    = "claim-topics-registry"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
odra-casper-livenet-env = { workspace = true, optional = true }
odra         = { workspace = true }
odra-modules = { workspace = true }

[dev-dependencies]
odra-test = { workspace = true }

[features]
livenet = ["odra-casper-livenet-env"]
